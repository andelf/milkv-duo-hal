use crate::pac;

// We need to export this in the hal for the drivers to use

crate::peripherals! {
    UART0 <= UART0,
    UART1 <= UART1,
    UART2 <= UART2,
    UART3 <= UART3,
    UART4 <= UART4,



}
